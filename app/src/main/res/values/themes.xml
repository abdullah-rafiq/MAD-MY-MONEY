<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyMoney" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="fullscreenBackgroundColor">@color/primaryColor</item>
        <item name="fullscreenTextColor">@color/primaryColor</item>
        <item name="android:windowBackground">@color/primaryColor</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyMoney" parent="Base.Theme.MyMoney" />

    <style name="ThemeOverlay.MyMoney.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/primaryColor</item>
        <item name="fullscreenTextColor">@color/primaryColor</item>
    </style>
</resources>